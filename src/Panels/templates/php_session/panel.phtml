<?php declare(strict_types=1);

/** @var \Jwebas\Debugger\Support\Panel $this */
/** @var array $data */

?>

<style class="tracy-debug">
    table.tracy-table.table-phpSession td.e {
        width : 160px;
    }

    table.tracy-table.table-phpSession td.v {
        max-width : 400px;
    }
</style>

<?php foreach ($data as $key => $datum): ?>
    <span style="font-weight: bold;">
        <?= $datum['title'] ?>
    </span>

    <table class="tracy-table tracy-sortable table-phpSession">
        <thead>
        <tr>
            <th>Key</th>
            <th>Value</th>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($datum['values'] as $title => $value): ?>
            <tr>
                <td class="e"><?= $title ?></td>
                <td class="v"><?= $this->toHtml($value) ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
<?php endforeach; ?>
