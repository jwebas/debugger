<?php

use Jwebas\Debugger\Panels\SlimEnvironmentPanel;

/** @var SlimEnvironmentPanel $this */

?>

<style class="tracy-debug">
    .tracy-SlimEnvironment table.tracy-table .e {
        width : 130px;
    }

    .tracy-SlimEnvironment table.tracy-table .v {
        max-width : 400px;
    }
</style>

<h1><?= $this->getIcon() ?> <?= $this->getTitle() ?></h1>

<div class="tracy-inner tracy-SlimEnvironment">
    <div class="tracy-inner-container">

        <table class="tracy-table tracy-sortable">
            <thead>
            <tr>
                <th>Key</th>
                <th>Value</th>
            </tr>
            </thead>
            <tbody>
            <?php foreach ($this->getData() as $title => $value): ?>
                <tr>
                    <td class="e"><?= $title ?></td>
                    <td class="v"><?= $this->toHtml($value) ?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>
    </div>
</div>
