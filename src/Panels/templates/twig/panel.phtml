<?php declare(strict_types=1);

use Jwebas\Debugger\Panels\Abstracts\AbstractPanel;

/** @var $this AbstractPanel */
/** @var string $panelTitle */
/** @var array $data */

?>

<style class="tracy-debug">
    .tracy-TwigPanel table.tracy-table .e {
        width : 120px;
    }

    .tracy-TwigPanel table.tracy-table .v {
        max-width : 500px;
    }
</style>

<h1><?= $panelTitle ?></h1>

<div class="tracy-inner tracy-TwigPanel">
    <div class="tracy-inner-container">

        <span style="font-weight: bold;">Extensions</span>

        <table class="tracy-table tracy-sortable">
            <thead>
            <tr>
                <th>Extension</th>
                <th>Data</th>
            </tr>
            </thead>
            <tbody>
            <?php foreach ($data['extensions'] as $title => $value): ?>
                <tr>
                    <td class="e"><?= $title ?></td>
                    <td class="v"><?= $this->toHtml($value) ?></td>
                </tr>
            <?php endforeach; ?>
            </tbody>
        </table>

        <span style="font-weight: bold;">Twig Loader</span>
        <div style="margin-bottom: 15px;"><?= $this->toHtml($data['loader']) ?></div>

        <span style="font-weight: bold;">Twig</span>
        <div style="margin-bottom: 15px;"><?= $this->toHtml($data['full']) ?></div>

        <span style="font-weight: bold;">Twig Profile</span>
        <div><?= $this->toHtml($data['twigProfile']) ?></div>

    </div>
</div>
